name: Lottery Sync

on:
  workflow_dispatch:
  schedule:
    - cron: '0 21 * * 1,4'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Replace with your sync script."
